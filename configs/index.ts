import databaseConfig, {
  DB_CONF_KEY,
  DBConfigType,
} from "./database.configuration";
import serverConfig, {
  SERVER_CONF_KEY,
  ServerConfigType,
} from "./server.configuration";

export {
  databaseConfig,
  DB_CONF_KEY,
  DBConfigType,
  serverConfig,
  SERVER_CONF_KEY,
  ServerConfigType,
};
